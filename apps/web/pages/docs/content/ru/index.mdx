---
title: Добро пожаловать в нашу документацию
description: Исчерпывающие руководства и документация, которые помогут вам начать работу с нашим продуктом
---

# Начало работы с TypeScript

## Введение
TypeScript - это мощное расширение JavaScript, добавляющее статическую типизацию. Давайте рассмотрим основные концепции.

### Пример кода - НОВОЕ
```typescript title="example.ts"
// Этот код можно будет скопировать
const приветствие = "Привет, Мир!";
console.log(приветствие);
```

```typescript
// Обычный код
const пример = "привет";
```

```typescript showLineNumbers {1,3-4} title="example.ts"
// Код с нумерацией и подсветкой строк
const x = 1;
const y = 2;
console.log(x + y);
```

```diff
- const старыйКод = "удалить";
+ const новыйКод = "добавить";
```

### Пример кода

```typescript
interface Пользователь {
  id: number;
  имя: string;
  email?: string; // Необязательное свойство
}

// Функция с аннотациями типов
function приветствоватьПользователя(пользователь: Пользователь): string {
  return `Привет, ${пользователь.имя}!`;
}

// Пример использования
const новыйПользователь: Пользователь = {
  id: 1,
  имя: "Иван Петров"
};

console.log(приветствоватьПользователя(новыйПользователь)); // Вывод: Привет, Иван Петров!
```

## Таблица типов данных

| Тип      | Пример          | Описание                     |
|----------|-----------------|------------------------------|
| string   | "Привет"        | Текстовые данные             |
| number   | 42, 3.14        | Числовые значения            |
| boolean  | true/false      | Логические значения          |
| array    | [1, 2, 3]       | Упорядоченная коллекция      |

## Важные заметки

> 💡 TypeScript компилируется в JavaScript, что делает его совместимым со всеми средами JavaScript.

### Список возможностей
- Вывод типов
- Объявление интерфейсов
- Необязательные свойства
- Обобщенные типы

## Пример математики
Вот простое математическое уравнение:

$$ E = mc^2 $$

# Встроенные формулы
Это формула внутри текста: $E = mc^2$

# Блочные формулы
$$
\begin{equation}
\frac{n!}{k!(n-k)!} = \binom{n}{k}
\end{equation}
$$

# Сложные формулы
$$
\begin{align*}
& \phi(x,y) = \phi \left(\sum_{i=1}^n x_ie_i, \sum_{j=1}^n y_je_j \right)
= \sum_{i=1}^n \sum_{j=1}^n x_i y_j \phi(e_i, e_j) = \\
& (x_1, \ldots, x_n) \left( \begin{array}{ccc}
\phi(e_1,e_1) & \cdots & \phi(e_1,e_n) \\
\vdots & \ddots & \vdots \\
\phi(e_n,e_1) & \cdots & \phi(e_n,e_n)
\end{array} \right)
\left( \begin{array}{c}
y_1 \\
\vdots \\
y_n
\end{array} \right)
\end{align*}
$$

## Пример компонента
Вы можете использовать React компоненты прямо в MDX:

```tsx
import { useState } from 'react'

export function Counter() {
  const [count, setCount] = useState(0)

  return (
    <button onClick={() => setCount(c => c + 1)}>
      Счет: {count}
    </button>
  )
}
```

Вот как его использовать:

<Counter />

# Пример кода с подсветкой

```javascript {1,3} /console.log/
const x = 1;
const y = 2;
console.log(x + y);
```

В этом примере:
- Первая и третья строки будут подсвечены (через `{1,3}`)
- Слово `console.log` будет дополнительно выделено (через `/console.log/`)
- В углу будет показано "javascript"
- Будет применена тема github-dark
- Строки будут разделены сеткой

Вы можете настроить стили для классов `highlighted` и `word--highlighted` в вашем CSS чтобы определить как именно будут выглядеть подсвеченные элементы.

# Базовый пример
```js
console.log('привет!')
```

# С нумерацией строк и подсветкой
```js showLineNumbers {3} title="example.js"
const x = 1
const y = 2
console.log(x + y)
```

# С метаданными для подсветки диапазона строк
```js {1,3-4}
import lib from 'lib'
const a = 1
const b = 2
console.log(a + b)
```

# С подсветкой слов
```js /привет/
console.log('привет!')
```
